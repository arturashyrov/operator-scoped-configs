server:
  port: 7777

someConfig:
  foo: default-bar

redcasino:
  someConfig:
    foo: red-bar

bluecasino:
  someConfig:
    foo: blue-bar
    secretValue: '{cipher}'

# curl localhost:8888/encrypt -d superSecretValue

