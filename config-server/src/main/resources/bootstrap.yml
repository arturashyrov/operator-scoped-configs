server:
  port: 8888

spring:
  config:
    name: config-server
  cloud:
    config:
      failFast: true
      server:
        git:
          uri: https://github.com/arturashyrov/operator-scoped-configs
          searchPaths: configs-samples
          cloneOnStart: true